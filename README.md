# 📱 Projeto Redes Sociais (Estudo de Front-End)

Este é um projeto de estudo de front-end que simula a visualização de perfis de redes sociais na tela de um smartphone.

O objetivo foi praticar o uso de **iframes** para carregar conteúdo dinâmico e **técnicas de posicionamento e transformação** em CSS para criar a moldura do celular.

## 🚀 Conceitos Praticados

* **HTML5:** Estruturação semântica e uso de `iframes`.
* **CSS3:**
    * Posicionamento avançado (`position: absolute`, `transform: translate(-50%, -50%)`).
    * Estilização de fundos (`background-size: cover`, `background-attachment: fixed`).
    * Animações de `hover` com `transition` e `transform`.
    * Design "mobile-first" (neste caso, "mobile-only").
